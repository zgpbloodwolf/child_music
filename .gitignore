# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
*.local

# 环境变量(本地/线上后端地址各自填写,不入库;模板见 .env.example)
.env

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# resource/ 是原始资源档案层:只保留说明文件(约定要能被团队看到),
# 里面的原始音频共约 727MB,整目录排除、切不可入库
/resource/*
!/resource/README.md
src/static/library/
.hbuilderx/launch.json

# 后端服务(server/)
server/.env
server/.venv/
# 仓库根也曾建过一个 venv(仅 setuptools/pkg_resources,约 128MB),同样不入库
/.venv/
server/storage/
server/data/
server/**/__pycache__/
server/**/*.pyc

# 打包传输用的大文件(710MB 音频包,单独传服务器,不入库)
src/static.zip
.trae/

# CodeGraph 本地代码知识图谱索引(codegraph init 生成,每个机器各自一份)
.codegraph/
.claude/settings.json
__pycache__/
.workbuddy/

# 脚本生成的中间产物(server/scripts/import-erge.py 输出的导入映射表)
import-plan.json

